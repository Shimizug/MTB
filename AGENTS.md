# AGENTS.md

このファイルは、Codexが本リポジトリを編集するときに従う共通ルールを定義します。

## プロジェクト概要

本プロジェクトは、目標、タスク、アンチタスク、ご褒美を一元管理し、AIによるタスク提案や振り返りを支援するWebアプリケーションです。

## 技術スタック

- バックエンド: C# / .NET 10 / ASP.NET Core Web API
- データアクセス: Entity Framework Core
- バックエンドテスト: xUnit
- フロントエンド: Vue.js / TypeScript / Vite
- データベース: PostgreSQL
- コンテナ: Podman / Podman Compose
- クラウド: Microsoft Azure
- インフラ: Terraform
- CI/CD: GitHub Actions

READMEに記載された技術スタックと異なるフレームワークを、明示的な依頼なしに追加しないでください。

## ディレクトリ構成

- `backend/`: ASP.NET Coreバックエンドとバックエンドテスト
- `frontend/`: Vue.jsフロントエンドとフロントエンドテスト
- `sql/`: PostgreSQL用のSQL、初期データ、補助スクリプト
- `infra/`: Terraformなどのインフラ定義
- `docs/`: 設計資料、仕様書、運用資料
- `compose.yml`: ローカル開発用コンテナ構成

## 共通の編集方針

- 変更前に関連する既存コード、設定、ドキュメントを確認してください。
- 依頼された範囲に集中し、無関係なリファクタリングを含めないでください。
- 既存の命名規則、設計、フォーマットがある場合は、それを優先してください。
- 秘密情報、APIキー、パスワード、接続文字列をコミットしないでください。
- 環境固有の値は環境変数またはローカル設定ファイルで管理し、必要に応じてサンプルファイルを用意してください。
- 新しい依存パッケージは必要最小限にし、追加理由を説明してください。
- 仕様やセットアップ方法が変わる場合は、関連するドキュメントも更新してください。
- 文字コードはUTF-8、改行コードは原則LFを使用してください。

## バックエンド方針

- Nullable Reference Typesを有効にし、nullの扱いを明確にしてください。
- 非同期I/Oには`async` / `await`を使用し、公開非同期メソッドには`Async`接尾辞を付けてください。
- APIの入力値を検証し、適切なHTTPステータスコードを返してください。
- Controllerに業務ロジックを集中させず、責務を適切に分離してください。
- Entity Framework Coreのマイグレーションは、モデル変更と整合する状態にしてください。
- PostgreSQL固有の挙動や型を利用する場合は、理由が分かるようにしてください。
- 新機能や不具合修正には、可能な限りxUnitによるテストを追加してください。

## フロントエンド方針

- Vue 3のComposition APIと`<script setup lang="ts">`を基本とします。
- TypeScriptの型を明示し、安易に`any`を使用しないでください。
- コンポーネントは責務を小さく保ち、再利用可能なロジックはcomposableへ分離してください。
- API通信、ローディング、空状態、エラー状態を考慮してください。
- PCとスマートフォンの双方で利用できるレスポンシブUIにしてください。
- アクセシビリティを考慮し、意味のあるHTML要素、ラベル、キーボード操作を使用してください。

## データベース方針

- 主データベースにはPostgreSQLを使用してください。
- テーブル名、カラム名、制約、インデックスは用途が分かる名前にしてください。
- 外部キー、NOT NULL、UNIQUEなどの整合性制約をデータベースにも定義してください。
- 破壊的なスキーマ変更には、移行方法と既存データへの影響を明記してください。
- SQLは可能な限り再実行時の影響を考慮してください。

## 検証

変更後は、変更範囲に応じて次の確認を行ってください。

- バックエンドの復元、ビルド、テスト
- フロントエンドの依存関係の確認、型チェック、テスト、ビルド
- Entity Framework CoreマイグレーションとPostgreSQLスキーマの整合性確認
- `compose.yml`を変更した場合は、構成の検証
- Terraformを変更した場合は、フォーマットと構文の検証

実行できなかった検証がある場合は、完了報告に理由と未確認事項を記載してください。

## Git運用

- ユーザーの既存変更を上書きまたは取り消さないでください。
- 生成物、秘密情報、ローカルデータをステージしないでください。
- コミットやプッシュは、ユーザーから明示的に依頼された場合のみ行ってください。
- コミットは目的ごとにまとめ、変更内容が分かるメッセージを使用してください。

## 完了報告

- 実装または修正した内容を簡潔に説明してください。
- 変更した主要ファイルを示してください。
- 実行した検証と結果を示してください。
- 残っている課題や利用者側で必要な作業があれば明記してください。
